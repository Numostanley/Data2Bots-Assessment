{
   "user": {
      "type": "object",
      "properties": {
         "id": {
            "type": "string",
            "tag": "Id",
            "description": "The Id attribute. Represented by a(n) string data type.",
            "required": false
         },
         "nickname": {
            "type": "string",
            "tag": "Nickname",
            "description": "The Nickname attribute. Represented by a(n) string data type.",
            "required": false
         },
         "title": {
            "type": "string",
            "tag": "Title",
            "description": "The Title attribute. Represented by a(n) string data type.",
            "required": false
         },
         "accountType": {
            "type": "string",
            "tag": "Accounttype",
            "description": "The Accounttype attribute. Represented by a(n) string data type.",
            "required": false
         },
         "countryCode": {
            "type": "string",
            "tag": "Countrycode",
            "description": "The Countrycode attribute. Represented by a(n) string data type.",
            "required": false
         },
         "orientation": {
            "type": "string",
            "tag": "Orientation",
            "description": "The Orientation attribute. Represented by a(n) string data type.",
            "required": false
         }
      },
      "tag": "User",
      "description": "The User attribute. Represented by a(n) object data type.",
      "required": false
   },
   "time": {
      "type": "integer",
      "tag": "Time",
      "description": "The Time attribute. Represented by a(n) integer data type.",
      "required": false
   },
   "acl": {
      "type": "list",
      "tag": "Acl",
      "description": "The Acl attribute. Represented by a(n) list data type.",
      "required": false
   },
   "publicFeed": {
      "type": "bool",
      "tag": "Publicfeed",
      "description": "The Publicfeed attribute. Represented by a(n) bool data type.",
      "required": false
   },
   "internationalCountries": {
      "type": "enum",
      "items": {
         "type": "string"
      },
      "tag": "Internationalcountries",
      "description": "The Internationalcountries attribute. Represented by a(n) enum data type.",
      "required": false
   },
   "topTraderFeed": {
      "type": "bool",
      "tag": "Toptraderfeed",
      "description": "The Toptraderfeed attribute. Represented by a(n) bool data type.",
      "required": false
   }
}